# Shawn authorizes QA Assist to merge verified PRs and deploy through the
# independent Mac controller. Ad hoc live-router mutations remain prohibited.
[merge]
enabled = true

[verify]
test = "pytest -q"
timeout_seconds = 900

[promotion]
enabled = true
stages = [{ name = "production", timeout_seconds = 900 }]
