# local-qwen35 — fully offline. Claude Code -> backdoor -> Ollama -> local model.
# Default offline brain: Qwen3.5 4B at a SANE 64K context (256K-native model,
# native tool-calling). This is the model the FULL HARNESS loads onto — both the
# `qwen` wrapper (full mode) and `/model qwen` via the :8083 hybrid router.
# Why 4B (2026-06-18): the 9B held the ~44-50K harness prompt fine on KV, but a
# cold prefill of that prompt on the 9B still took minutes before first token.
# The 4B prefills the same context several times faster, so the harness is
# actually usable. 64K holds the prompt with headroom; 256K-native, so 64K has
# no rope penalty. (The older 9b-256k variant was a separate, worse problem: its
# ~16GB q8_0 KV loaded ~22GB on the 36GB M5 Max and thrashed.)
# Pair with MCP OFF in full mode (the ~142K of MCP schemas was the other half of
# the slowness, and wouldn't fit 64K anyway) — see the qwen wrapper.
# Requires `ollama serve` running and `ollama pull qwen3.5:4b` +
# `ollama create qwen3.5:4b-64k -f modelfiles/qwen3.5-4b-64k.Modelfile`.

PROVIDER_BASE_URL=http://localhost:11434/v1
# Dummy key — Ollama ignores it, but bd's guard rejects empty/placeholder values.
PROVIDER_API_KEY=ollama
PROVIDER_MODEL=qwen3.5:4b-64k

# Mirror the vendor defaults shipped in the model's Modelfile (temp 1.0,
# top_p 0.95, top_k 20, presence_penalty 1.5). Request-level params override
# Modelfile ones, so these must MATCH — don't reuse qwen2.5's 0.2 here (low
# temp causes repetition loops on the hybrid-reasoning qwen3.x models).
PROVIDER_MAX_TOKENS=16384
PROVIDER_TEMPERATURE=1.0
PROVIDER_TOP_P=0.95

# Thinking OFF for agentic use: qwen3.5 with thinking enabled intermittently
# ends a turn with reasoning ONLY (no visible text) → empty assistant message,
# and every turn pays the reasoning-token latency. Set to empty to re-enable
# thinking (the proxy will surface it as Anthropic thinking blocks, with a
# reasoning-only safety net), but expect slower turns.
PROVIDER_REASONING_EFFORT=none

# ── Server ───────────────────────────────────────────────────────────────────
HOST=127.0.0.1
PORT=8082
LOG_FILE=proxy.log

# ── Request optimizations (also reduce local compute on a laptop) ────────────
SKIP_QUOTA_PROBES=true
SKIP_TITLE_GENERATION=true
SKIP_SUGGESTION_MODE=true
MOCK_PREFIX_DETECTION=true
MOCK_FILEPATH_EXTRACTION=true

CLAUDE_WORKSPACE=./workspace
MAX_CLI_SESSIONS=5
